{
  "name": "qrstream-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:wasm": "cd wasm && PATH=\"$HOME/.rustup/toolchains/stable-aarch64-apple-darwin/bin:$PATH\" wasm-pack build --target web --release",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:wasm": "node tests/wasm-smoke.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-wasm": "^3.4.0"
  },
  "dependencies": {
    "zxing-wasm": "^3.1.3"
  }
}
