[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "qrstream-rs"
version = "0.1.0"
description = "Rust V4 core prototype for QRStream"
requires-python = ">=3.10"

[tool.maturin]
features = ["python"]
